# Vanilla Command Palette UI

A clean, modern front-end micro-dashboard built with **pure HTML, CSS, and JavaScript** — no frameworks, no dependencies.

This project is a developer-focused UI demo featuring:

- Glassmorphism card layout  
- Command palette (**Ctrl+K**)  
- Keyboard-first UX  
- Tag filtering + live search  
- Dark/light theme toggle  
- Toast notifications  
- Responsive grid design  

---

## ✨ Preview

This UI is designed to feel like a real product dashboard — the kind of front-end developers enjoy exploring.

---

## 🚀 Features

✅ **Command Palette**  
Press **Ctrl + K** to open a command menu (inspired by VS Code + Linear).

✅ **Keyboard Shortcuts**

| Key | Action |
|-----|--------|
| `/` | Focus search |
| `Ctrl + K` | Open command palette |
| `Esc` | Close palette |
| `Enter` | Open card action |

✅ **Live Search + Tag Filtering**  
Instant filtering of cards without reloads.

✅ **Theme Toggle**  
Dark/light mode with saved preference in `localStorage`.

✅ **Clean Design System**  
Uses CSS variables for easy customization.

---

## 📁 Project Structure

wow-ui/
│
├── index.html
├── css/
│ └── styles.css
└── js/
└── app.js


---

## 🛠️ Getting Started

### 1. Clone the repo

```bash
git clone https://github.com/YOURNAME/vanilla-command-palette-ui.git
cd vanilla-command-palette-ui
